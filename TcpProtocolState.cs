namespace lib61850net
{
        public enum TcpProtocolState
        {
            TCP_STATE_START,
            TCP_CONNECT_WAIT,
            TCP_CONNECTED,
            TCP_STATE_SHUTDOWN
        }
}
