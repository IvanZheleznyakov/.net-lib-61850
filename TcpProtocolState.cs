namespace lib61850net
{
        public enum TcpProtocolState
        {
            TCP_STATE_START,
            TCP_CONNECT_WAIT,
            TCP_CONNECTED,
            TCP_STATE_SHUTDOWN,
            TCP_STATE_CLOSING,
            TCP_STATE_CLOSED
        }
}
